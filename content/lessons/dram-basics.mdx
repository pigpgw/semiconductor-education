---
title: "DRAM은 왜 빠른 작업 메모리인가"
description: "DRAM 셀 구조, 휘발성, refresh, DDR/LPDDR/GDDR/HBM으로 이어지는 흐름을 기초부터 정리합니다."
level: "기초"
category: "메모리"
tags:
  - "DRAM"
  - "Memory"
  - "Refresh"
publishedAt: "2026-05-07"
updatedAt: "2026-05-07"
learningGoals:
  - "DRAM이 데이터를 전하로 저장한다는 사실을 설명할 수 있다"
  - "휘발성과 refresh가 왜 필요한지 말할 수 있다"
  - "DDR, LPDDR, GDDR, HBM의 차이를 용도 관점에서 구분할 수 있다"
prerequisites:
  - "0과 1의 기본 개념"
  - "CPU와 메모리의 큰 역할"
sources:
  - title: "DRAM | 메모리 | 삼성반도체"
    url: "https://semiconductor.samsung.com/kr/dram/"
  - title: "DRAM | Memory | Samsung Semiconductor Global"
    url: "https://semiconductor.samsung.com/dram/"
---

<OneLine>
DRAM은 CPU와 GPU가 바로 꺼내 쓸 데이터를 잠시 맡아 두는 빠른 작업대입니다.
</OneLine>

## 쉬운 비유

컴퓨터를 책상에서 공부하는 사람으로 생각해 봅시다.

SSD는 책장입니다. 많은 자료를 오래 보관하지만, 매번 책장에서 꺼내면 시간이 걸립니다.

DRAM은 책상 위 공간입니다. 지금 읽는 책, 필기구, 메모를 올려두면 바로 손이 닿습니다. 대신 책상 위 물건은 정리하거나 전원이 꺼지면 사라진다고 보면 됩니다.

그래서 DRAM은 저장장치가 아니라 **작업 중인 데이터를 빠르게 제공하는 메모리**입니다.

## 실제 구조

DRAM의 기본 셀은 보통 **1T1C**라고 부릅니다.

- **1T**는 하나의 트랜지스터입니다.
- **1C**는 하나의 커패시터입니다.
- 커패시터에 전하가 있으면 1, 부족하면 0으로 해석합니다.
- 트랜지스터는 그 전하를 읽고 쓰는 문 역할을 합니다.

이 구조가 중요한 이유는 단순합니다. 셀 하나가 작아야 같은 면적에 더 많은 비트를 넣을 수 있습니다. DRAM은 이 단순한 구조 덕분에 큰 용량을 비교적 좋은 비용으로 만들 수 있습니다.

<Term title="1T1C">
1 transistor 1 capacitor의 줄임말입니다. DRAM 셀을 이루는 가장 기본적인 구조를 뜻합니다.
</Term>

## 왜 refresh가 필요한가

커패시터에 담긴 전하는 영원히 남아 있지 않습니다. 시간이 지나면 조금씩 새어 나갑니다.

그래서 DRAM은 데이터를 유지하기 위해 주기적으로 다시 읽고 써 주는 동작이 필요합니다. 이 동작을 **refresh**라고 부릅니다.

refresh는 DRAM의 약점처럼 보이지만, 동시에 DRAM의 성격을 잘 보여줍니다. SRAM처럼 복잡한 셀을 쓰지 않고, 단순한 셀을 촘촘히 배치해 큰 용량을 얻는 대신 주기적인 관리가 필요한 구조입니다.

## 용도별 DRAM

DRAM은 하나의 제품만 뜻하지 않습니다. 같은 원리를 바탕으로 용도에 맞게 여러 제품군으로 나뉩니다.

| 종류 | 주 사용처 | 중요한 기준 |
| --- | --- | --- |
| DDR | PC, 서버 | 용량, 대역폭, 안정성 |
| LPDDR | 스마트폰, 모바일 기기 | 낮은 전력, 얇은 폼팩터 |
| GDDR | 그래픽카드 | 그래픽 처리용 대역폭 |
| HBM | AI 가속기, HPC | 매우 넓은 대역폭, 적층 구조 |

여기서 HBM도 DRAM 계열이라는 점이 중요합니다. HBM은 완전히 다른 메모리가 아니라, 여러 DRAM 칩을 수직으로 쌓고 넓게 연결해 대역폭을 크게 키운 제품군입니다.

<IndustryKeywords items="1T1C|Refresh|Volatile memory|DDR|LPDDR|GDDR|HBM" />

## 현업 관점에서 보는 핵심

DRAM을 이해할 때는 “빠르다” 하나로 끝내면 부족합니다.

현업에서는 보통 다음 질문으로 봅니다.

- 같은 면적에 얼마나 많은 비트를 넣을 수 있는가
- CPU나 GPU에 데이터를 얼마나 빨리 공급할 수 있는가
- 전력과 발열은 감당 가능한가
- 서버, 모바일, 그래픽, AI 중 어디에 최적화됐는가

삼성반도체의 DRAM 설명에서도 DDR, LPDDR, GDDR, HBM이 서로 다른 용도에 맞게 쓰인다는 흐름을 확인할 수 있습니다. 이 관점은 메모리를 단순 부품이 아니라 시스템 성능을 좌우하는 자원으로 보게 만듭니다.

## 공식 자료 기반 근거

삼성반도체는 DRAM을 하나의 트랜지스터와 하나의 커패시터로 구성된 셀들의 집합으로 설명합니다. 또한 DRAM은 CPU와 GPU가 필요한 데이터를 빠르게 가져올 수 있도록 일시적으로 보관하는 휘발성 메모리라고 설명합니다.

이 글에서는 그 설명을 초보자 관점으로 다시 풀었습니다. 핵심은 DRAM이 “영구 저장”이 아니라 “작업 중 빠른 접근”에 최적화된 메모리라는 점입니다.

<CheckQuestions questions="DRAM이 전원이 꺼지면 데이터를 잃는 이유는 무엇인가요?|refresh는 DRAM의 어떤 물리적 한계를 보완하나요?|HBM이 DRAM 계열인데도 AI에서 특별히 중요한 이유는 무엇일까요?" />
